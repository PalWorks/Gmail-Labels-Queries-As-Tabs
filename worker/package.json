{
    "name": "gmail-tabs-feedback-worker",
    "version": "1.0.0",
    "private": true,
    "description": "Cloudflare Worker that relays in-extension feedback to email via Resend.",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250109.0",
        "typescript": "^5.3.3"
    }
}
