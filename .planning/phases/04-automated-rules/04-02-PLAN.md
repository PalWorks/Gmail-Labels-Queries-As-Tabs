---
phase: 04-automated-rules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/options.html, src/options.css, src/options.ts, manifest.json, build.js]
autonomous: false
requirements: [FEAT-05, FEAT-09, FEAT-10, FEAT-11]

must_haves:
  truths:
    - "Options page opens from chrome-extension URL or right-click → Options"
    - "Left sidebar navigation shows all 6 menu items with icons"
    - "Clicking nav items switches the right content pane"
    - "Dark theme matches extension branding"
    - "Existing Gmail modal is unaffected"
  artifacts:
    - path: "src/options.html"
      provides: "Options page HTML structure"
      contains: "Automation Rules"
    - path: "src/options.ts"
      provides: "Options page navigation and routing logic"
      min_lines: 50
    - path: "src/options.css"
      provides: "Options page styles with dark theme"
      min_lines: 100
  key_links:
    - from: "manifest.json"
      to: "src/options.html"
      via: "options_page or options_ui declaration"
      pattern: "options"
    - from: "build.js"
      to: "src/options.ts"
      via: "esbuild entry point"
      pattern: "options"
---

<objective>
Create the Options page scaffold with navigation, routing, and all section placeholders.

Purpose: Standalone admin panel page that houses all new features (Automation Rules, User Guide, etc.) alongside existing Settings.
Output: Working options.html with sidebar navigation, dark theme CSS, and content switching logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automated-rules/04-CONTEXT.md

@src/welcome.html
@src/welcome.ts
@manifest.json
@build.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create options.html with two-column layout and all sections</name>
  <files>src/options.html, src/options.css</files>
  <action>
  Create src/options.html:
  1. Two-column layout: fixed left sidebar (240px) + scrollable right content area
  2. Left sidebar navigation with icon + label for each menu item:
     - ⚙️ Settings (default active)
     - ⚡ Automation Rules
     - 📖 User Guide
     - 🔒 Privacy
     - 💬 Get in Touch
     - 📋 Logs / History
  3. Each nav item has data-section attribute matching its content panel
  4. Right side: 6 content panels (divs), only one visible at a time
  5. Section placeholders:
     - Settings: container div that will mirror Gmail modal (placeholder text for now)
     - Automation Rules: container with header "⚡ Automation Rules" + subtitle + placeholder
     - User Guide: container with numbered step list (placeholder content)
     - Privacy: container with privacy policy text (extension makes zero external requests, etc.)
     - Get in Touch: container with placeholder for Tally embed
     - Logs / History: container with info about Google Sheet logging + how to check
  6. Link to options.css and options.js (output from build)
  7. Include Google Fonts (Inter) via CDN link
  
  Create src/options.css:
  1. Dark theme: body bg #1a1a2e, sidebar bg #16213e, content bg #1a1a2e
  2. Sidebar: fixed position, full height, 240px width
     - Nav items: padding, hover state (lighten bg), active state (blue left border + blue text)
     - Icons: inline, consistent size
  3. Content area: margin-left 240px, padding 40px
  4. Typography: Inter font, white text, muted gray for secondary text
  5. Card styles: #16213e bg, rounded corners (8px), subtle border
  6. Form controls consistent with extension branding:
     - Inputs: dark bg, lighter border, white text
     - Selects: styled dark dropdowns
     - Toggles: custom CSS toggle switches (green for on, gray for off)
     - Buttons: blue accent (#0f3460) with hover states
  7. Responsive: sidebar collapses to icons-only on narrow viewports
  </action>
  <verify>Open options.html in browser, visually inspect layout</verify>
  <done>Two-column layout renders correctly with all 6 sections navigable</done>
</task>

<task type="auto">
  <name>Task 2: Create options.ts with navigation routing and build integration</name>
  <files>src/options.ts, build.js, manifest.json</files>
  <action>
  Create src/options.ts:
  1. Hash-based routing: clicking nav item sets window.location.hash (e.g., #settings, #rules)
  2. On load and hashchange: show corresponding content panel, hide others
  3. Update active nav item styling
  4. Default to #settings if no hash
  5. Load account settings from chrome.storage.sync (reuse getSettings from storage.ts)
  
  Update build.js:
  1. Add 'src/options.ts' to esbuild entry points array
  2. Ensure output goes to dist/js/options.js
  
  Update manifest.json:
  1. Add options_page or options_ui pointing to options.html
  2. Use "options_page": "options.html" for full-page options (not popup)
  3. Copy options.html and options.css to dist during build (add to file copy list if needed)
  4. Do NOT change any permissions — this is purely a UI addition
  </action>
  <verify>npm run build succeeds, open chrome-extension://ID/options.html</verify>
  <done>Options page loads, navigation works, manifest declares options page</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Options page scaffold with sidebar navigation and 6 sections — extension loaded at chrome-extension URL</what-built>
  <how-to-verify>
  1. Right-click extension icon → Options (or navigate to chrome-extension://ID/options.html)
  2. Verify: dark theme, left sidebar with 6 nav items, right content area
  3. Click each nav item — corresponding section should show, others hide
  4. Verify the existing Gmail inbox modal is still working (open Gmail, click extension icon)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds with no errors
- [ ] options.html loads in Chrome via chrome-extension URL
- [ ] All 6 navigation items switch content correctly
- [ ] Dark theme renders as designed
- [ ] manifest.json includes options_page declaration
- [ ] Existing Gmail modal still works (no regression)
</verification>

<success_criteria>
- All tasks completed
- User approved the visual layout
- No regressions to existing functionality
- Options page is accessible from Chrome extension management
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-rules/04-02-SUMMARY.md`
</output>
