---
phase: 04-automated-rules
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: [src/options.ts, src/options.html, src/options.css]
autonomous: false
requirements: [FEAT-05, FEAT-07, FEAT-08, FEAT-09, FEAT-10, FEAT-11, FEAT-12]

must_haves:
  truths:
    - "Settings section displays user's current tab list and theme"
    - "Automation Rules section shows per-tab rule config with action dropdown, days input, toggle"
    - "Generate Script button produces valid Apps Script and copies to clipboard"
    - "User Guide section shows clear step-by-step bulleted instructions"
    - "Privacy section explains zero-external-request policy"
    - "Get in Touch section has placeholder for Tally embed"
    - "Logs / History section explains Google Sheet logging setup"
  artifacts:
    - path: "src/options.ts"
      provides: "Full options page logic with rules UI and script generation"
      min_lines: 200
    - path: "src/options.html"
      provides: "Complete options page with all sections populated"
      contains: "Generate"
  key_links:
    - from: "src/options.ts"
      to: "src/modules/rules.ts"
      via: "imports generateAppsScript"
      pattern: "import.*generateAppsScript.*rules"
    - from: "src/options.ts"
      to: "src/utils/storage.ts"
      via: "imports getSettings, addRule, updateRule"
      pattern: "import.*getSettings.*storage"
---

<objective>
Populate all Options page sections with real content and functionality.

Purpose: Wire up the data layer (Plan 01) with the UI scaffold (Plan 02) to create the complete user-facing experience.
Output: All 6 sections fully functional — Settings mirror, Automation Rules with script generation, User Guide, Privacy, Get in Touch, Logs/History.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automated-rules/04-CONTEXT.md
@.planning/phases/04-automated-rules/04-01-SUMMARY.md
@.planning/phases/04-automated-rules/04-02-SUMMARY.md

@src/utils/storage.ts
@src/modules/rules.ts
@src/options.ts
@src/options.html
@src/options.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Automation Rules section with rule config UI</name>
  <files>src/options.ts, src/options.html, src/options.css</files>
  <action>
  In the Automation Rules section:
  
  1. On load: fetch user settings (getSettings) to get tabs and existing rules
  2. Render a rule config row for each tab:
     - Tab name label (read-only)
     - Action dropdown: Trash, Archive, Mark as Read, Move to Label
     - "After X days" number input (min=1, default=30)
     - Enable/disable toggle switch
     - If action = "Move to Label": show additional text input for target label
  3. Auto-save rules on change (debounced, using addRule/updateRule from storage.ts)
  4. At the bottom: "🚀 Generate & Copy Script" button
     - On click: call generateAppsScript() from rules.ts with enabled rules
     - Copy result to clipboard (navigator.clipboard.writeText)
     - Show success feedback: button text changes to "✅ Copied!" for 3 seconds
     - Also show a modal/expandable area with the full script for review
  5. Below the button: "📖 View Setup Guide" link that navigates to #user-guide
  6. Handle edge case: no tabs configured yet → show friendly message
  7. Handle Google Sheet URL input: optional text field above the generate button
     "Paste your Google Sheet URL for activity logging (optional)"
  </action>
  <verify>Build + load extension, configure rules, click Generate</verify>
  <done>Rules UI renders all tabs, saves to storage, generates valid script</done>
</task>

<task type="auto">
  <name>Task 2: Populate Settings, User Guide, Privacy, Get in Touch, and Logs sections</name>
  <files>src/options.ts, src/options.html, src/options.css</files>
  <action>
  Settings section:
  1. Show current account's tab list (read from storage)
  2. Show theme selector (system/light/dark) — syncs with storage
  3. Show unread count toggle — syncs with storage
  4. Note: full tab editing stays in the Gmail modal; this is a read/configure view
  
  User Guide section:
  1. Header: "How to Set Up Email Automation"
  2. Simple numbered list with clear formatting:
     Step 1: Configure your rules in the Automation Rules section above
     Step 2: Click "Generate & Copy Script" to copy the Apps Script
     Step 3: Open script.google.com — link button opens in new tab
     Step 4: Create a new project, select all (Ctrl+A), paste (Ctrl+V), save (Ctrl+S)
     Step 5: Click Triggers (clock icon) → Add trigger → autoCleanup → Time-driven → Day timer
     Step 6: Authorize when prompted — click "Advanced" → "Go to autoCleanup" → Allow
  3. Brief FAQ section:
     - "How often does it run?" → Based on your trigger (daily recommended)
     - "Is it safe?" → Uses trash (recoverable 30 days), never permanent delete
     - "Can I stop it?" → Delete the trigger in Apps Script
  
  Privacy section:
  1. Clear statement: "This extension makes zero external network requests"
  2. Explain data storage: all settings in chrome.storage.sync (browser-local)
  3. Explain Apps Script: runs under YOUR Google account, extension doesn't have access
  4. No analytics, no tracking, no data collection
  
  Get in Touch section:
  1. Placeholder div for Tally form embed
  2. Text: "Have feedback or need help? We'd love to hear from you."
  3. Include a note: "Form will be added soon" (until Tally URL is provided)
  
  Logs / History section:
  1. Explanation: "Your automation history is logged to a Google Sheet"
  2. Instructions on how to set up Sheet logging:
     - Create a blank Google Sheet
     - Copy its URL
     - Paste into the "Sheet URL" field in Automation Rules
  3. Link button: "Open Google Apps Script Dashboard" → script.google.com
  4. Explain: users can check execution history in Apps Script editor → Executions tab
  </action>
  <verify>Build + review all sections in Chrome</verify>
  <done>All 6 sections populated with real content</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Options page with all 6 sections functional — loaded as Chrome extension</what-built>
  <how-to-verify>
  1. Open Options page (right-click extension → Options)
  2. Settings: verify account settings display correctly
  3. Automation Rules: configure a rule, set days, enable toggle, click Generate & Copy Script
  4. Paste copied script into a text editor — verify it looks like valid Apps Script
  5. User Guide: verify clear numbered steps
  6. Privacy: verify zero-request policy is explained
  7. Get in Touch: verify placeholder
  8. Logs / History: verify Sheet logging instructions
  9. Open Gmail: verify existing tab bar + modal still work (no regression)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] All 6 sections render with real content
- [ ] Rules save to chrome.storage.sync correctly
- [ ] Script generation works and copies to clipboard
- [ ] User Guide is clear and actionable
- [ ] No regressions to Gmail inbox functionality
</verification>

<success_criteria>
- All tasks completed
- User approved the complete Options page
- Script generation produces valid Apps Script
- No regressions to existing extension functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-automated-rules/04-03-SUMMARY.md`
</output>
